#include <bits/stdc++.h>
#include "testlib.h"
 
using namespace std;

int main(int argc, char* argv[]) {
  registerValidation(argc, argv);
  int t = inf.readInt(1,100000,"t");
  inf.readEoln();
  for(int tt=1;tt<=t;tt++) {
    setTestCase(tt);
    bool isUpper=false;
    string s = inf.readToken("[a-zA-Z]{1,10}", "s");
    inf.readEoln();
  }
  inf.readEof();
}




