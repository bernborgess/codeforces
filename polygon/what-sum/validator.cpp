#include <bits/stdc++.h>
#include "testlib.h"
 
using namespace std;
 
int main(int argc, char* argv[]) {
  registerValidation(argc, argv);
  int n = inf.readInt(0, 2048, "n");
  inf.readEoln();
  for(int i=0;i<n;i++) {
    if(i)
    inf.readSpace();
    int e = inf.readInt(0,2048,"e");
  }
  inf.readEoln();
  inf.readEof();
}

